import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lektorgt',
  appName: 'lektor-front',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
